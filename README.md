# AI Pipeline Research & GraphQL Exploration Prototype

This repository contains the local prototype deliverables for the AI Pipeline research task, focusing on local CodeBERT embedding generation and GitHub GraphQL schema validation.

---

## 🛠️ Local Environment & Pipeline Setup

1. **Virtual Environment Configuration:**
   Managed dependencies using a isolated local environment:
   ```cmd
   python -m venv venv
   venv\Scripts\activate